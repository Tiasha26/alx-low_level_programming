#include <stdio.h>
/**
 * main -Starting pont
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char alph;
	for (aplh = 'a'; aplh <= '2'; aplh++)
	{
		putchar(aplh);
	}
	putchar('\n');
	return (0);
}
