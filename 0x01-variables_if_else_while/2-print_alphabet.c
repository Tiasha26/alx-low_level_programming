#include <stdio.h>
/**
 * main -Starting pont
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char alph;
	for (alph = 'a'; alph <= '2'; alph++)
	{
		putchar(alph);
	}
	putchar('\n');
	return (0);
}
